/**
 * API 與應用設定（部署時請修改 API_BASE_URL）
 */
const CONFIG = {
  /** 後端位址，例如 http://127.0.0.1:5000 */
  API_BASE_URL: "http://127.0.0.1:5000",
  /**
   * 完整 ee-class 前端（Vue）網址，點選課程時導向。
   * 留空則僅提示，不跳轉。
   */
  MAIN_APP_URL: "",
};
